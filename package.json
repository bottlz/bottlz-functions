{
  "name": "bottlz-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "func start",
    "format": "prettier '**/*.{js,scss,css,html,md}' --write",
    "format:check": "prettier '**/*.{js,scss,css,html,md}' --check",
    "lint": "eslint ./route-function --fix --ext .js",
    "lint:check": "eslint . --ext .js"
  },
  "dependencies": {
    "azure-maps-control": "^2.2.0",
    "azure-maps-rest": "^2.1.0",
    "dotenv": "^16.0.3",
    "geojson": "^0.5.0"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "eslint-config-prettier": "^8.5.0",
    "prettier": "^2.8.0"
  }
}
